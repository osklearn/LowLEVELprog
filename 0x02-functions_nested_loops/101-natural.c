#include <stdio.h>
/**
* main - print thesum of all multiples of 3 or 5 below 1024
*
*Return: 0
*/

int main(void)
{
	int i, n, m, sum;

	sum = 0;
	for (i=0; i < 1024; i++)
	{
		n = i % 3;
		m = i % 5;
		if (a == 0 || b == 0)
			sum = sum + i;
	}
	printf("%d\n", sum);
	return (0);
}
